{
  "name": "forex-bot",
  "private": true,
  "scripts": {
    "dev": "node -e \"require('http').createServer((_,r)=>{r.setHeader('Content-Type','text/html');r.end('<h1>Forex Bot (Python)</h1><p>This project is a Python trading bot — no web app.</p>')}).listen(8080)\"",
    "build": "echo built",
    "build:dev": "echo built"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.110.2"
  }
}
